<h1>Payment Status for <%= @year %></h1>

<table class="table">
  <thead>
    <tr>
      <% @months.each do |month| %>
        <th><%= month %></th>
      <% end %>
    </tr>
  </thead>
  <tbody>
    <tr>
      <% @months.each do |month| %>
        <% payment = @payments.find { |p| p.month == month } %>
        <td>
          <% if payment %>
            <p>Status: <%= payment.status.capitalize %></p>
          <% else %>
            <p>No payment data</p>
          <% end %>
        </td>
      <% end %>
    </tr>
  </tbody>
</table>

<%= link_to 'Back to Profile', profile_path, class: 'btn btn-secondary' %>
